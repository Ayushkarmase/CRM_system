# Backend API URL — set this to your Railway backend service URL before building.
# Example: VITE_API_URL=https://your-backend-service.up.railway.app
# Leave blank in development (Vite proxy handles /api/* → localhost:8000)
VITE_API_URL=
